# Serve deterministic mock predictions; set to false once the trained model lands in predict/model.py
MOCK_MODE=true
# Comma-separated browser origins allowed by CORS (add the university server origin when deploying)
CORS_ORIGINS=http://localhost:3000
