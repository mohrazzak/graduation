# Exact pins of the verified working set in api/.venv (pip freeze, direct deps
# only) — demo-day insurance against a breaking release landing overnight.
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pillow==12.2.0
pydantic==2.13.4
