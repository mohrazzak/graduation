# Heavy, OPTIONAL detector dependencies. Deliberately NOT in requirements.txt:
# the base Docker image and the free-tier cloud deployment run the mock backend,
# which needs none of this. Install locally to run the real model:
#
#   .venv/bin/pip install -r requirements-models.txt
torch==2.12.0
ultralytics==8.4.56
numpy>=1.26
