# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Env — never commit real credentials
.env
.env.local
.env*.local

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
*.swp
# Claude Code session-recovery runtime state
HANDOFF.md
.claude/settings.local.json
.claude/rate-limit-state.json
.claude/stop-failure-events.jsonl
.claude/quota-blocked.json

# Model weights — never in the repo. The serving checkpoint lives outside it,
# at RAED_WEIGHTS_PATH.
*.pt

# Reference clone, deleted 2026-09-02. Kept ignored so a re-clone for
# reference can never be committed — it holds a hardcoded API credential.
A-Smart-Site-For-Rehabilitating-Damaged-Buildings/

# Local Claude session-recovery config
session-recover.yaml
.env.bak*

# Runtime logs and pidfiles written by scripts/dev-all.sh
.run/
